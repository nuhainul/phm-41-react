const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

const divideTheFirstNumberByTheSecondOne = (first, second) => {
    return first / second;
}

export {add, multiply, divideTheFirstNumberByTheSecondOne}

// export default add;

// const calculator = { add: add }
// export default calculator;